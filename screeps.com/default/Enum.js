
function Enum(name, enums)
{
	this.name = name
	this.count = enums.length

	for (let index = 0, num = this.count; index < num; ++index)
	{
		this[key] = index
	}
}

module.exports = Enum
